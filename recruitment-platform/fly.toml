# Fly.io deployment config
# Deploy: fly launch (first time) or fly deploy (updates)
# Docs: https://fly.io/docs/reference/configuration/

app = "sq-saiyou"
primary_region = "nrt"   # Tokyo

[build]
  dockerfile = "Dockerfile"

[env]
  PORT       = "3000"
  NODE_OPTIONS = "--experimental-sqlite"
  SITE_URL   = "https://saiyou.social-quality.com"
  COMPANY_NAME = "株式会社Social Quality"
  SITE_NAME  = "採用サイト"

[http_service]
  internal_port = 3000
  force_https   = true
  auto_stop_machines  = true
  auto_start_machines = true
  min_machines_running = 1

  [http_service.concurrency]
    type       = "requests"
    soft_limit = 100
    hard_limit = 200

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1

# Persistent volume for SQLite data
[[mounts]]
  source      = "recruitment_data"
  destination = "/app/data"
  initial_size = "1gb"
