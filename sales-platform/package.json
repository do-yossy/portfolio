{
  "name": "sales-platform",
  "version": "1.0.0",
  "description": "営業バックエンド: 案件パイプライン × 売上100万 × 学習（受注管理・スコアリング・見積・提案・LP問い合わせ受付）",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "dev:local": "node --watch --experimental-sqlite --env-file=.env server.js",
    "start:local": "node --experimental-sqlite --env-file=.env server.js",
    "seed": "node seed.js",
    "test": "node test.js",
    "sync": "node scripts/sync-sheet.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "googleapis": "^144.0.0"
  }
}
